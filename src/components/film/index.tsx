import React, { FC } from 'react';

interface IProps {};
interface IState {};

const Film: FC = (props: IProps, state: IState) => {
  return (
    <div>
      <p>Film</p>
    </div>
  )
}

export default Film;
